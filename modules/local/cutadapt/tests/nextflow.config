process {
    withName : CUTADAPT{
        ext.args    = { [
            meta.sequencer in ["NovaSeq","NextSeq"] ? "--nextseq-trim=20 -m 50" : "--max-n 0 -m 36",
            "-q 20,20"
        ].join(' ').trim() }
    }
}
